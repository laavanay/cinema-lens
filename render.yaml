services:
  - type: web
    name: cinema-lens
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn cinema_lens.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.10.13"
      - key: ALLOWED_HOSTS
        sync: false
      - key: MODEL_DIR
        value: "./models"
    healthCheckPath: /api/health/

